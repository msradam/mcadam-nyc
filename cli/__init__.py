"""Mcadam CLI prototype.

Python implementation of the six routing tools, mirrored from the eventual
Rust → WASM router. Used as the source of truth for routing semantics, the
test fixtures, and the cost-profile JSON DSL.
"""

__version__ = "0.0.1"
